{
  "name": "sample-lib",
  "version": "0.0.0",
  "description": "Sample Package",
  "type": "module",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest run",
    "lint": "eslint",
    "build": "del-cli dist && tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "del-cli": "^6.0.0"
  }
}
